# Must learn
- advenced file operation (fgets, fputs, fflush, ...)
- dirent.h header
- sys/stat.h header

# Ask claude
Break it one by one:

## Only the static recursive system first
- the get full_path is inside the parsing(config) system?

## Now the dynamic recursive system